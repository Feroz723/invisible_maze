services:
  - type: web
    name: invisible-maze
    env: node
    buildCommand: npm install -g tsx && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    plan: free
    autoDeploy: true
